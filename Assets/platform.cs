using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Program : MonoBehaviour
{

    //var RedCar = new UI_script();
    void Start()
    {
        //RedCar.name = "Моя тачка";
        //Debug.Log(RedCar.name);
    }

    void Update()
    {
        //bool to_rotate = UI_script.to_rotate;
        //Debug.Log(to_rotate);
        //if (to_rotate)
        //{
            //transform.Rotate(transform.up * 360f * Time.deltaTime);
        //}
    }
}
